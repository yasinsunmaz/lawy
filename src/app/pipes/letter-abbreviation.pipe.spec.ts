import { LetterAbbreviationPipe } from './letter-abbreviation.pipe';

describe('LetterAbbreviationPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterAbbreviationPipe();
    expect(pipe).toBeTruthy();
  });
});
